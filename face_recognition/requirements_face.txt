# Use a Python 3.12 venv on Windows:
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   pip install -r requirements_face.txt
#
# dlib-bin = prebuilt dlib (avoids Visual C++ compile on Windows)
dlib-bin>=19.24.0; platform_system=="Windows"
face_recognition==1.3.0
opencv-python>=4.9.0
numpy>=1.26
requests>=2.31.0
setuptools>=65,<81
